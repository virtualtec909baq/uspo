.ibox-title
  table.table-responsive.table.table-striped.table-bordered.table-hover
    thead
      tr
        th Key
        th Value
        
    tbody
      - @settins1.each do |settin|
        tr
          td = settin.key
          - if settin.has_image
            td 
             = settin.image.url
          - else
            td 
              = settin.value
          td = link_to 'Edit', edit_settin_path(settin)